name: "Peercoin Core"
version: "0.15.11"
slug: "peercoin"
description: "Peercoin Core ARM64 avec minting continu"
arch:
  - aarch64

startup: services
boot: auto
init: false

ports:
  9901/tcp: 9901

ports_description:
  9901/tcp: "Peercoin réseau principal"

options:
  rpcuser: "ppc_rpc"
  rpcpassword: "CHANGEZ_CE_MOT_DE_PASSE_RPC"
  walletname: "android-legacy"
  walletpassphrase: ""
  minting: true

schema:
  rpcuser: str
  rpcpassword: password
  walletname: str
  walletpassphrase: password
  minting: bool
